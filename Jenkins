pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Hello World'
            }
        }
        stage('Build') {
            steps {
                sh ./gradlew clean build
            }
        }
    }
}
